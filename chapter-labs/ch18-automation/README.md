# Chapter 18 Lab — Observable CI Automation

Build a bounded headless workflow with explicit configuration, structured output, budget/time limits, harness validation, telemetry, independent verifier, loop prevention, and fail-closed recovery.

Starting state: `ch16-secure-ready` plus the completed Chapter 17 recovery lab state.
Milestone: `ch18-ci-ready`.
